version: '3'

services:
    php:
        command: tail -f /dev/null
        build:
            context: '.'
            args:
                PHP_VERSION: '${DEV_PHP_VERSION}'
        volumes:
            - '.:/usr/src/app'
        command: 'tail -f /dev/null'
